version: '2'

services:
  nginx:
    image: nginx:1.11.1
    restart: unless-stopped
    volumes:
      - ./nginx-ll100.conf:/etc/nginx/conf.d/ll100.conf
      - ${LL100_SHARED_DATA_DIR}/portal/nginx/html:/usr/share/nginx/html
    environment:
      - VIRTUAL_HOST=www.ll100.com,support.ll100.com,dev.ll100.com
    networks:
      - nginx-proxy

networks:
  nginx-proxy:
    external: true
